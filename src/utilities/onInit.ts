import { Payload } from 'payload';
import process from 'node:process';
import { revalidatePath, revalidateTag } from 'next/cache'


export const onInit = async (payload: Payload): Promise<void> => {
}